#include "binary_tree/binary_tree.hpp"
#include <vector>

using namespace tree;

// TODO: need to implement tree with nodes having parent pointers
//  template <typename T>
//  vector<node_ptr<T>> inorder(const node_ptr<T> &root)
//  {
//      vector<node_ptr<T>> res;
//      auto cur = root, prev = nullptr;
//      while (cur)
//      {
//      }
//      return res;
//  }